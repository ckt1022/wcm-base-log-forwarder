# This file is automatically generated by componentize-py
# It is not intended for manual editing.
"""
parse component interface
"""
from typing import TypeVar, Generic, Union, Optional, Protocol, Tuple, List, Any, Self, Callable
from types import TracebackType
from enum import Flag, Enum, auto
from dataclasses import dataclass
from abc import abstractmethod
import weakref

from componentize_py_types import Result, Ok, Err, Some
from .imports import parse_process


class WitWorld(Protocol):

    @abstractmethod
    def parse(self, raw_data: List[bytes]) -> List[parse_process.LogEntry]:
        """
        核心解析函數
        傳入 [][]byte (list<list<u8>>)
        回傳 結構化結果的列表，或是一個解析錯誤
        
        Raises: `wit_world.types.Err(wit_world.imports.parse_process.ParseError)`
        """
        raise NotImplementedError

    @abstractmethod
    def report_usage(self) -> int:
        raise NotImplementedError

