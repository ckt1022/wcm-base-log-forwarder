// Program.cs — wasi-wasm entry point.
// WIT export functions (filter / report-usage) are wired through generated ABI
// glue in ReductionPlugin.cs ([UnmanagedCallersOnly] methods).
// ReductionPluginWorldExportsImpl.cs holds the actual implementation.

class Program { static void Main() { } }
