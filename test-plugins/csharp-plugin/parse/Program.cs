// Program.cs — wasi-wasm entry point.
// The WIT export functions (parse / report-usage / reset) are wired through
// the generated ABI glue in ParserPlugin.cs  ([UnmanagedCallersOnly] methods).
// ParserPluginWorldExportsImpl.cs holds the actual implementation.

class Program { static void Main() { } }
