version: "3.8"
services:
  vector:
    image: timberio/vector:latest-alpine
    container_name: vector
    volumes:
      - ./vector.toml:/etc/vector/vector.toml:ro
      - ./lua:/etc/vector/lua:ro
      - ./test-logs.log:/etc/vector/test-logs.log
    command: ["--config", "/etc/vector/vector.toml"]
